<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Visio.ViewModels"
             x:Class="Visio.Views.MainPage"
             x:DataType="vm:MainViewModel">

    <Grid RowDefinitions="Auto,*,Auto" Padding="20" RowSpacing="15">
        
        <VerticalStackLayout Grid.Row="0" Spacing="10">
            <Label Text="URL RTSP:" 
                   FontSize="16" 
                   FontAttributes="Bold"/>
            
            <Entry Text="{Binding RtspUrl}" 
                   Placeholder="rtsp://exemplo.com/stream"
                   IsEnabled="{Binding IsConnected, Converter={StaticResource InvertedBoolConverter}}"
                   FontSize="14"/>
            
            <HorizontalStackLayout Spacing="10">
                <Button Text="Conectar" 
                        Command="{Binding ConnectCommand}"
                        IsEnabled="{Binding IsConnected, Converter={StaticResource InvertedBoolConverter}}"
                        BackgroundColor="Green"
                        TextColor="White"
                        WidthRequest="120"/>
                
                <Button Text="Desconectar" 
                        Command="{Binding DisconnectCommand}"
                        IsEnabled="{Binding IsConnected}"
                        BackgroundColor="Red"
                        TextColor="White"
                        WidthRequest="120"/>
            </HorizontalStackLayout>
            
            <Label Text="{Binding StatusMessage}" 
                   TextColor="{Binding StatusColor}"
                   FontSize="14"
                   FontAttributes="Bold"/>
        </VerticalStackLayout>
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0"
                   Source="{Binding CurrentFrame}" 
                   Aspect="AspectFit"
                   BackgroundColor="Black"
                   HorizontalOptions="Fill"
                   VerticalOptions="Fill"/>

            <ScrollView Grid.Column="1">
                <VerticalStackLayout Padding="15" Spacing="10">
                    
                    <Label Text="Captura" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           Margin="0,0,0,5"/>
                    
                    <Button Text="📸 Capturar Foto"
                            Command="{Binding CaptureSnapshotCommand}"
                            BackgroundColor="#2196F3"
                            TextColor="White"
                            HeightRequest="45"/>
                    
                    <BoxView HeightRequest="1" 
                             Color="Gray" 
                             Margin="0,15,0,10"/>
                    
                    <Label Text="Filtros" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           Margin="0,0,0,5"/>
                    
                    <HorizontalStackLayout Spacing="10">
                        <CheckBox IsChecked="{Binding IsGrayscaleEnabled}"/>
                        <Label Text="Escala de Cinza" 
                               VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                    
                    <HorizontalStackLayout Spacing="10">
                        <CheckBox IsChecked="{Binding IsBlurEnabled}"/>
                        <Label Text="Desfoque" 
                               VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                    
                    <HorizontalStackLayout Spacing="10">
                        <CheckBox IsChecked="{Binding IsEdgeDetectionEnabled}"/>
                        <Label Text="Detecção de Bordas" 
                               VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                    
                </VerticalStackLayout>
            </ScrollView>
        </Grid>
        
    </Grid>
</ContentPage>
